# ثبت چک‌لیست از مرورگر در گیت‌لب

این نسخه در همان پروژهٔ `fb_git_amirmahdi/checklist`، برای هر شماره‌سریال یک فایل Markdown نگه می‌دارد. کاربر فرم را در GitLab Pages باز می‌کند؛ دکمهٔ ثبت مستقیماً از طریق API گیت‌لب روی شاخهٔ `main` یک Commit می‌سازد. در این گردش کار، Clone، Git محلی، نصب برنامه یا اجرای Python لازم نیست.

## استفاده

1. [فرم وب](http://pages.faraabin.ir/fb_git_amirmahdi/checklist/Form.html) را باز کنید. پس از انتشار نسخهٔ جدید، در صورت نمایش فرم قدیمی یک بار `Ctrl+F5` بزنید.
2. در حساب خود وارد GitLab شوید و از بخش **Preferences / Access Tokens** یا [صفحهٔ توکن شخصی](http://gitlab.faraabin.ir/-/user_settings/personal_access_tokens) یک Personal Access Token دارای دسترسی **api** و تاریخ انقضا بسازید. حساب باید اجازهٔ Push روی `main` همین مخزن داشته باشد. ورود عادی به گیت‌لب به‌تنهایی به صفحهٔ Pages اجازهٔ نوشتن نمی‌دهد.
3. توکن را فقط داخل کادر «توکن دسترسی شخصی» فرم قرار دهید و **اتصال** را بزنید. آن را در فایل HTML، کد پروژه، Markdown یا پیام‌ها قرار ندهید.
4. شماره‌سریال را وارد کنید. برای ادامهٔ کار قبلی، **استعلام و بارگذاری سوابق** را بزنید. بارگذاری با تأیید شما مقادیر ویرایش‌شدهٔ فعلی را جایگزین می‌کند.
5. اطلاعات را تغییر دهید و **ثبت اطلاعات در گیت‌لب** را بزنید.
6. بعد از پیام موفقیت، پیوندهای **مشاهدهٔ فایل Markdown** و **مشاهدهٔ Commit** نمایش داده می‌شوند.

مثال: سریال `210` در `records/atlas-f70/210.md` ذخیره می‌شود. ثبت بعدی همان فایل را با یک Commit تازه به‌روز می‌کند. تاریخچهٔ نسخه‌های قبلی در گیت‌لب باقی می‌ماند. شماره‌های فارسی و عربی سریال به رقم لاتین تبدیل می‌شوند؛ فاصله و `/` در سریال مجاز نیستند.

اگر هنوز توکن آماده نیست، دکمهٔ **دانلود نسخهٔ Markdown** خروجی را می‌سازد. دانلود صرفاً نسخهٔ محلی است و ثبت روی گیت‌لب محسوب نمی‌شود.

## آرشیو فایل‌ها و تقویم

پنل بالای سمت راست، مسیر `records/atlas-f70` را به پوشهٔ واقعی گیت‌لب پیوند می‌دهد. فهرست تمام فایل‌های Markdown با صفحه‌بندی از API خوانده می‌شود؛ جست‌وجو، شمارنده و تازه‌سازی دارد. در مخزن عمومی، مشاهدهٔ فهرست و بارگذاری فایل نیازی به توکن ندارد؛ نوشتن همچنان به توکن حساب مجاز نیاز دارد. برای مخزن خصوصی ابتدا متصل شوید.

یک کلیک، فایل را انتخاب می‌کند. دوبارکلیک یا Enter روی فایل، اطلاعات آن را در فرم باز می‌کند؛ دکمهٔ «بازکردن» نیز برای موبایل و استفاده با یک کلیک در دسترس است. اگر فرم تغییر ثبت‌نشده داشته باشد، قبل از جایگزینی تأیید گرفته می‌شود و با انصراف تمام مقادیر فعلی حفظ می‌شوند. دکمهٔ «چک‌لیست جدید» فرم خالی باز می‌کند. پس از ذخیره، فهرست به‌روز می‌شود.

تقویم شمسی با کلیک روی فیلد تاریخ باز می‌شود: ماه و سال قابل انتخاب است، «امروز» و «پاک‌کردن» دارد و کلیدهای جهت روی روزها عمل می‌کنند. Escape تقویم را می‌بندد. فیلدهای تاریخ همچنان ورودی دستی را می‌پذیرند. تقویم مستقل از CDN و مبتنی بر تقویم Persian در Intl مرورگر است؛ سال کبیسه و پایان ماه در آزمون‌ها بررسی می‌شوند.

## محتوای فایل

- بخش‌های مونتاژ، تولید، کالیبراسیون، صحت‌سنجی و تأییدها، مطابق ترتیب فرم.
- مقدار تمام ۷۵ فیلد فعلی؛ شامل تاریخ شمسی، متن و وضعیت همهٔ checkboxها.
- جدول‌های Markdown، متن فارسی و نشانه‌های ☑ و ☐، همراه با عنوان شماره‌سریال.
- یک بخش JSON در کامنت انتهای همان `.md` برای بارگذاری دقیق مجدد در فرم؛ فایل دادهٔ جداگانه ایجاد نمی‌شود.

ویرایش اطلاعات را از فرم انجام دهید. بارگذاری از کامنت دادهٔ انتهای فایل انجام می‌شود؛ ویرایش دستی متن نمایشی Markdown به فیلدهای فرم منتقل نمی‌شود. کامنت داده را حذف نکنید. اگر فایل فاقد دادهٔ قابل‌خواندن باشد، فرم آن را خودکار بازنویسی نمی‌کند. قالب ظاهری Markdown تابع نمایشگر گیت‌لب است و کپی پیکسلی HTML نیست.

## حفظ اطلاعات و برخورد با خطا

- اگر فایلی از قبل وجود داشته باشد، ثبت بدون بارگذاری قبلی آن متوقف می‌شود.
- قبل از به‌روزرسانی، آخرین Commit فایل با نسخهٔ بارگذاری‌شده مقایسه می‌شود. درخواست نوشتن نیز `last_commit_id` دارد تا تغییر هم‌زمان در فاصلهٔ خواندن و نوشتن شناسایی شود.
- در تعارض، تغییرات فعلی داخل فرم باقی می‌ماند. ابتدا Markdown فعلی را دانلود کنید، سپس سوابق را دوباره بخوانید و تغییرات را آگاهانه اعمال کنید.
- اگر پاسخ ثبت قطع شود، ممکن است Commit در سرور ایجاد شده باشد. ثبت دوباره ابتدا محتوای سرور را مقایسه می‌کند؛ پاسخ نامشخص به معنی شکست قطعی نیست.
- اگر محتوا تغییری نکرده باشد، Commit تکراری ساخته نمی‌شود.
- دکمه‌ها هنگام درخواست غیرفعال‌اند تا ثبت دوبار انجام نشود. هنگام خروج با تغییرات ثبت‌نشده، مرورگر هشدار می‌دهد.
- پیش‌نویس فرم روی دیسک مرورگر ذخیره نمی‌شود؛ بستن صفحه بدون ثبت یا دانلود می‌تواند تغییرات را از بین ببرد.

## دسترسی و ارتباط

توکن فقط در حافظهٔ صفحه نگه داشته می‌شود؛ در localStorage، sessionStorage، URL یا مخزن ذخیره نمی‌شود. کادر توکن پس از زدن اتصال پاک می‌شود. با بارگذاری مجدد صفحه باید دوباره متصل شوید. «قطع اتصال» توکن را از صفحه حذف می‌کند؛ برای ابطال خود توکن از تنظیمات حساب گیت‌لب استفاده کنید.

آدرس فعلی GitLab و Pages از **HTTP** استفاده می‌کند؛ توکن در مسیر شبکه رمزگذاری نمی‌شود. برای استفادهٔ امن با حساب واقعی، HTTPS معتبر را برای هر دو سرویس فعال کنید و آدرس‌های `CONFIG.host` و پیوند ساخت توکن را به HTTPS تغییر دهید. HTTPS فقط برای Pages کافی نیست. برای میزبانی پروژه‌های غیرقابل‌اعتماد روی همین Pages، جداسازی دامنهٔ هر پروژه نیز باید در تنظیمات سرور بررسی شود.

مجوز `api` برای نوشتن با این API لازم است؛ `read_api` و `read_repository` کافی نیستند. نقش Developer لزوماً اجازهٔ Push به شاخهٔ محافظت‌شدهٔ `main` ندارد؛ قانون همان شاخه تعیین‌کننده است. این برنامه مجوز مخزن را تغییر نمی‌دهد. فایل‌های ذخیره‌شده، همان سطح مشاهده‌پذیری مخزن را دارند.

هنگام توسعه، پاسخ عمومی پروژه با شناسهٔ `231` و شاخهٔ `main` و پذیرش درخواست CORS از `http://pages.faraabin.ir` روی سرور بررسی شد. اگر بعداً ارتباط مرورگر مسدود شد، پنل Network را بررسی کنید؛ خطای DNS، تغییر مسیر HTTP به HTTPS یا تنظیم CORS ممکن است علت باشد. توکن را برای رفع CORS در URL قرار ندهید و امنیت مرورگر را غیرفعال نکنید.

## فایل‌های پیاده‌سازی و انتشار

- `Form.html`: فرم و بخش ورود توکن.
- `assets/checklist.js`: تنظیمات مقصد، ساخت Markdown، خواندن فایل و ثبت Commit.
- `assets/workspace.css`: رابط فارسی، پنل آرشیو و چیدمان واکنش‌گرا.
- `assets/persian-calendar.js`: انتخابگر تاریخ شمسی مستقل از کتابخانهٔ خارجی.
- `assets/vendor/`: فونت محلی وزیرمتن و مجوز OFL؛ هیچ JavaScript خارجی هنگام استفاده دریافت نمی‌شود.
- `.gitlab-ci.yml`: انتشار `Form.html` به `public/index.html` و `public/Form.html`، به‌همراه پوشهٔ `assets` با Runner ویندوزی PowerShell.

فایل‌های `records` در خروجی Pages کپی نمی‌شوند؛ فرم آن‌ها را از API مخزن می‌خواند. پیام Commit داده‌ها شامل `[skip ci]` است تا هر ثبت فرم موجب انتشار مجدد سایت نشود. تغییر کد فرم و ارسال آن به `main`، Pipeline معمول انتشار را اجرا می‌کند. کاربران نهایی هیچ‌وقت لازم نیست پروژه را Clone کنند.

## آزمون توسعه

آزمون مرورگر با API شبیه‌سازی‌شده، بدون توکن واقعی و بدون نوشتن در سرور اجرا می‌شود:

```text
npm install
npx playwright install chromium
npm test
```

یا مسیر مرورگر موجود را در متغیر `BROWSER_EXECUTABLE` بدهید. آزمون‌ها ذخیره و بازیابی هر ۷۵ فیلد، فارسی و نویسه‌های خاص، ایجاد و ویرایش، تعارض، قطع پاسخ، توکن نامعتبر، دسترسی ناکافی، جلوگیری از Commit تکراری، نمایش موبایل و نبود کتابخانهٔ تقویم را پوشش می‌دهند. نتایج تصویری و نمونهٔ `.md` داخل `test-results` قرار می‌گیرند و وارد Git نمی‌شوند.

آزمون نهایی عملی نیازمند توکن حساب مجاز شماست: یک سریال آزمایشی وارد کنید، ثبت و سپس بارگذاری کنید و فایل و تاریخچهٔ Commit آن را در گیت‌لب ببینید.

## مستندات API

- [خواندن فایل مخزن و دسترسی موردنیاز](https://docs.gitlab.com/api/repository_files/)
- [ساخت Commit و کنترل last_commit_id](https://docs.gitlab.com/api/commits/#create-a-commit)
